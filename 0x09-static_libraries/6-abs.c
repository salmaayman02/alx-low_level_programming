#include "main.h"

/**
 * _abs - abs vslue
 * @n: int value
 * Description : prints number abs value
 *
 * Return: value of r
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
		return (n);
}
